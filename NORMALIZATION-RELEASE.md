# Triadic Closure Corpus Normalization Release

## Release identity

- Program: Triadic Closure Corpus Normalization Program
- Program version: 1.0
- Release tag: `normalization-v1.0`
- Tag target: the final `main` commit produced by merging the global-verification pull request

The tag must not point to the pre-merge verification branch because the release record itself belongs inside the released tree.

## Completed sequence

The release completes the full normalization sequence:

- C1–C3 corpus controls;
- F1–F5 foundation and generic domain method;
- M1–M4 mathematical normalization;
- P1–P29 physics normalization;
- X1 physics–chemistry interface;
- CH1–CH10 chemistry normalization;
- global architecture and reading-order pass;
- global archive/delete and semantic-compression pass;
- global verification and release preparation.

## Release invariants

- `index.md` is the sole canonical reader path.
- Authority and claim standing remain semantic and claim-specific.
- Every numbered owner retains its established scope and frontier.
- The complete proton/electron relation is `6 pi^5 [1 + c(3 pi^4)^-2]`, with `3/2 <= c <= 9/4`; `6 pi^5` alone is not the complete ratio.
- X1 remains an unresolved interface gate: the atomic ground, shell-edge object, and dynamical scale separation are not constructed.
- CH1–CH10 remain conditional on the X1 interface debt and do not retroactively prove it.
- Git history is the archive; obsolete provenance containers are not retained in the live corpus.

## Verification result

The release audit found no required theory, standing, ownership, architecture, compression, or frontier changes. Canonical navigation, repository controls, inventory classification, and the normalized unit sequence are mutually consistent.

The immutable release tag is to be applied to the final merge commit of the global-verification pull request.