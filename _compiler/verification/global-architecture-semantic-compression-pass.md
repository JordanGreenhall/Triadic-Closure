# Global Architecture and Semantic Compression Pass

## Scope

This pass executes the normalization program's global architecture step under the strengthened semantic-compression rule.

Theory, claim standing, and canonical unit ownership were read-only.

## Architecture result

- `index.md` is the sole canonical reader path.
- `README.md` separates reader entry from executor entry.
- `SCHEMA.md` defines living document roles and removes historical/superseded as permanent retention categories.
- `repository-inventory.md` distinguishes the live tree from the pending compression work queue.

## Compression result

Deleted:

- `overview/triadic-closure-reading-order.md`
- `overview/corpus-cleanup-pass-report.md`

The reading-order page duplicated the root index and omitted the completed chemistry sequence. Its useful orientation was already represented in the living controls.

The cleanup report contained only migration history and obsolete process records. Its surviving operational controls were already represented in the schema, lineage, status, work-plan, supersession, and execution files.

No useful semantic content was lost.

## Governing retention rule

The live corpus preserves semantics, not documents. Fully superseded containers are deleted. Partly useful containers must be decomposed, their useful material compressed into living owners, and the containers deleted after verification. Git history is the archive.

## Verification

- one canonical reader path: PASS;
- deleted files absent from governing navigation: PASS;
- repository topology updated to remove `overview/`: PASS;
- inventory updated: PASS;
- theory and claim standing unchanged: PASS;
- fresh-reader route begins at `index.md`: PASS.

## Remaining global compression work

The provenance-candidate queue in `repository-inventory.md` must be adjudicated document by document in the next archive/delete pass. Listing is not retention.
